@model IEnumerable<Teams>

<h1>School List</h1>

@foreach (var team in @Model)
{
    <tr>
        <td>@team.teamName</td>
        <td>@team.mascotName</td>
        <td>@team.conference</td>
    </tr>
}